"""The confirmation text the caller gets after hanging up.

    compose.py  what it says — the model writes the opening, code writes the facts
    telnyx.py   sending it, and the rules about when we refuse to

Same policy/vendor split as `booking/`: `compose.py` decides, `telnyx.py` delivers.
"""
