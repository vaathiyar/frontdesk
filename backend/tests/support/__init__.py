"""Test doubles and drivers. No tests live here.

fakes.py          ScriptedModel — the graph with no network and no API key
fake_calendar.py  an in-memory calendar; production has no such fallback
conversation.py   drives the graph by text, so the suite needs no LiveKit
"""
